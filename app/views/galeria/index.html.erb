<p id="notice"><%= notice %></p>

<h1>Galeria</h1>

<table class='table'>
  <thead>
    <tr>
      <th>Nome</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @galeria.each do |galerium| %>
      <tr>
        <td><%= galerium.nome %></td>
        <td><%= link_to 'Mostrar', galerium %></td>
        <td><%= link_to 'Editar', edit_galerium_path(galerium) %></td>
        <td><%= link_to 'Apagar', galerium, method: :delete, data: { confirm: 
        'Você tem certeza de que deseja apagar essa galeria?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Nova Galeria', new_galerium_path %>
